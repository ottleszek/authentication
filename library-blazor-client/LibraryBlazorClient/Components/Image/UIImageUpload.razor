@namespace LibraryBlazorClient.Components

<InputFile id="fileInput" OnChange="UploadImage" hidden />

<MudButton 
        Class="my-3"
        HtmlTag="label"
        Variant="Variant.Filled"
        Color="Color.Primary"
        StartIcon="@Icons.Filled.CloudUpload"
        for="fileInput">
        Kép feltöltése
</MudButton>
@if (ImgUrl is not null && ImgUrl!=string.Empty)
{
    <div>
        <img src="@ImgUrl" style="width:300px" />
    </div>
}